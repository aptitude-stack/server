name: aptitude-local

services:
  db:
    image: ${POSTGRES_IMAGE:-postgres:15.17}
    container_name: aptitude-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-aptitude}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-aptitude}"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 5s
    volumes:
      - aptitude-postgres-data:/var/lib/postgresql/data

  server:
    profiles: ["observability"]
    build:
      context: .
    image: ${APP_IMAGE:-y0ncha/aptitude-server:local}
    container_name: aptitude-server
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+psycopg://postgres:postgres@db:5432/aptitude
      AUTH_TOKENS_JSON: '{"reader-token":["read"],"publisher-token":["read","publish"],"admin-token":["read","publish","admin"]}'
      APP_ENV: container
      LOG_LEVEL: INFO
      LOG_FILE_PATH: /var/log/aptitude/app.jsonl
      PORT: 8000
      UVICORN_RELOAD: "false"
    ports:
      - "${APP_PORT:-8000}:8000"
    volumes:
      - aptitude-logs:/var/log/aptitude
    healthcheck:
      test: ["CMD-SHELL", "python -c \"from urllib.request import urlopen; urlopen('http://127.0.0.1:8000/healthz').read()\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

  migrate:
    profiles: ["observability"]
    build:
      context: .
    image: ${APP_IMAGE:-y0ncha/aptitude-server:local}
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+psycopg://postgres:postgres@db:5432/aptitude
      APP_ENV: container
    command: ["alembic", "upgrade", "head"]
    restart: "no"

  prometheus:
    profiles: ["observability"]
    image: ${PROMETHEUS_IMAGE:-prom/prometheus:v3.5.1}
    container_name: aptitude-prometheus
    depends_on:
      server:
        condition: service_started
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--web.enable-lifecycle"
    ports:
      - "${PROMETHEUS_PORT:-9090}:9090"
    volumes:
      - ./ops/monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./ops/monitoring/prometheus/alerts.yml:/etc/prometheus/alerts.yml:ro
      - prometheus-data:/prometheus

  loki:
    profiles: ["observability"]
    image: ${LOKI_IMAGE:-grafana/loki:3.6.0}
    container_name: aptitude-loki
    user: "0:0"
    command:
      - "-config.file=/etc/loki/config.yml"
    ports:
      - "${LOKI_PORT:-3100}:3100"
    volumes:
      - ./ops/monitoring/loki/config.yml:/etc/loki/config.yml:ro
      - loki-data:/loki

  promtail:
    profiles: ["observability"]
    image: ${PROMTAIL_IMAGE:-grafana/promtail:3.6.0}
    container_name: aptitude-promtail
    depends_on:
      server:
        condition: service_started
      loki:
        condition: service_started
    command:
      - "-config.file=/etc/promtail/config.yml"
    ports:
      - "${PROMTAIL_PORT:-9080}:9080"
    volumes:
      - ./ops/monitoring/promtail/config.yml:/etc/promtail/config.yml:ro
      - aptitude-logs:/var/log/aptitude:ro

  grafana:
    profiles: ["observability"]
    image: ${GRAFANA_IMAGE:-grafana/grafana-oss:11.6.0}
    container_name: aptitude-grafana
    depends_on:
      prometheus:
        condition: service_started
      loki:
        condition: service_started
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:-admin}
      GF_USERS_ALLOW_SIGN_UP: "false"
    ports:
      - "${GRAFANA_PORT:-3000}:3000"
    volumes:
      - ./ops/monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./ops/monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
      - grafana-data:/var/lib/grafana

volumes:
  aptitude-postgres-data:
  aptitude-logs:
  loki-data:
  prometheus-data:
  grafana-data:
